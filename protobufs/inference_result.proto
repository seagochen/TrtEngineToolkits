syntax = "proto3";

message InferenceResult {
    int64 timestamp = 1;            // 数据发送时间
    string delivered_from = 2;      // 数据来源方
    string infered_by = 3;          // 推理处理方
    string results = 4;             // 检测结果
}
