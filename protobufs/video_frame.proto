syntax = "proto3";

message VideoFrame {
    int64 timestamp = 1;        // 数据发送时间
    int32 width = 2;            // 数据帧的宽度
    int32 height = 3;           // 数据帧的高度
    int32 channels = 4;         // 数据帧的通道数
    bytes frame_data = 5;       // 数据帧
    string delivered_from = 6;  // 数据发布方
}
